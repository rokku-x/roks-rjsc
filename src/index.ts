// core exports remain on the root entry. Modal and Loading APIs are exported
// via dedicated subpaths to avoid duplication in the root package exports.

// Keep a minimal root export so the build produces a valid module and types.
export { }